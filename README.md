# StudentManagementSystem-JDBC
A console-based Java application that manages student records using JDBC and PostgreSQL. Features include adding, updating, deleting, and viewing student details through a simple text-based interface.
